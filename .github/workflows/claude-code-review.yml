name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    # 全ファイル対象でレビュー実行
    # paths制限なし - 全変更を対象

jobs:
  claude-review:
    # 全PRで実行 - 作成者に関係なく自動レビュー
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write  # コメント投稿に必要
      issues: read
      id-token: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run Claude Code Review
        id: claude-review
        uses: anthropics/claude-code-action@beta
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          github_token: ${{ secrets.GITHUB_TOKEN }}

          # Optional: Specify model (defaults to Claude Sonnet 4, uncomment for Claude Opus 4)
          # model: "claude-opus-4-20250514"
          
          # Direct prompt for automated review (no @claude mention needed)
          direct_prompt: |
            Please review this pull request and provide feedback on:
            - Code quality and best practices
            - Potential bugs or issues
            - Performance considerations
            - Security concerns
            - Test coverage
            
            Be constructive and helpful in your feedback.

          # sticky commentを有効化 - PR更新時に既存コメントを更新
          use_sticky_comment: true
          
          # Optional: Customize review based on file types
          # direct_prompt: |
          #   Review this PR focusing on:
          #   - For TypeScript files: Type safety and proper interface usage
          #   - For API endpoints: Security, input validation, and error handling
          #   - For React components: Performance, accessibility, and best practices
          #   - For tests: Coverage, edge cases, and test quality
          
          # Optional: Different prompts for different authors
          # direct_prompt: |
          #   ${{ github.event.pull_request.author_association == 'FIRST_TIME_CONTRIBUTOR' && 
          #   'Welcome! Please review this PR from a first-time contributor. Be encouraging and provide detailed explanations for any suggestions.' ||
          #   'Please provide a thorough code review focusing on our coding standards and best practices.' }}
          
          # プロジェクト特化ツール許可 - テスト・リント・型チェック実行可能
          allowed_tools: "Bash(npm test),Bash(npm run lint),Bash(npm run typecheck),Bash(npm run build),Read,Grep,Glob"
          
# レビュー実行条件 - [skip-review]タグがない限り実行
        # if文で条件制御（skip_ifパラメータは未サポートのため）
        if: |
          !contains(github.event.pull_request.title, '[skip-review]')

